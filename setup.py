from setuptools import setup
setup(
        name = 'postservices',
        packages = ['postservices'], # this must be the same as the name above
        version = '0.0.1',
        description = 'Python Library for Package Tracking',
        author = 'William Fligor',
        author_email = 'will@fligor.us',
        url = 'https://github.com/williamfligor/parcel-tracker', # use the URL to the github repo
        keywords = ['parcel', 'tracking'], # arbitrary keywords
        classifiers = [],
        )
